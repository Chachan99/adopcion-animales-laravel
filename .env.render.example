# ===========================================
# CONFIGURACIÓN PARA RENDER.COM
# ===========================================
# Copia este archivo y configura las variables según tu despliegue

# Aplicación
APP_NAME="Adopción de Animales"
APP_ENV=production
APP_KEY=base64:TU_CLAVE_GENERADA_AQUI
APP_DEBUG=false
APP_URL=https://tu-app.onrender.com

# Base de Datos PostgreSQL (Render)
# Obtén estos valores del dashboard de tu base de datos en Render
DB_CONNECTION=pgsql
DB_HOST=tu-host-postgresql.render.com
DB_PORT=5432
DB_DATABASE=tu_database_name
DB_USERNAME=tu_username
DB_PASSWORD=tu_password_seguro

# Cache y Sesiones
BROADCAST_DRIVER=log
CACHE_DRIVER=file
FILESYSTEM_DISK=local
QUEUE_CONNECTION=sync
SESSION_DRIVER=file
SESSION_LIFETIME=120

# Email (Gmail recomendado)
MAIL_MAILER=smtp
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=tu-email@gmail.com
MAIL_PASSWORD=tu-app-password
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS="tu-email@gmail.com"
MAIL_FROM_NAME="${APP_NAME}"

# Logging
LOG_CHANNEL=stack
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=error

# Configuraciones adicionales para Render
PORT=8080
NODE_ENV=production

# ===========================================
# INSTRUCCIONES DE CONFIGURACIÓN:
# ===========================================
# 
# 1. GENERAR APP_KEY:
#    php artisan key:generate --show
# 
# 2. CONFIGURAR BASE DE DATOS:
#    - Crear PostgreSQL database en Render
#    - Copiar la URL interna de conexión
#    - Extraer host, database, username, password
# 
# 3. CONFIGURAR EMAIL:
#    - Usar Gmail con App Password
#    - Habilitar 2FA en Gmail
#    - Generar App Password específica
# 
# 4. VARIABLES EN RENDER:
#    - Ir a Environment en tu Web Service
#    - Agregar cada variable una por una
#    - NO incluir comillas en los valores
# 
# ===========================================