# ===========================================
# CONFIGURACIÓN PARA VERCEL
# ===========================================
# Configura estas variables en el dashboard de Vercel

# Aplicación
APP_NAME="Adopción de Animales"
APP_ENV=production
APP_KEY=base64:TU_CLAVE_GENERADA_AQUI
APP_DEBUG=false
APP_URL=https://tu-app.vercel.app

# Base de Datos Externa (Neon.tech recomendado)
# Crear cuenta gratuita en https://neon.tech
DB_CONNECTION=pgsql
DB_HOST=tu-host.neon.tech
DB_PORT=5432
DB_DATABASE=tu_database
DB_USERNAME=tu_username
DB_PASSWORD=tu_password
DB_SSLMODE=require

# Cache y Sesiones (optimizado para serverless)
BROADCAST_DRIVER=log
CACHE_DRIVER=array
FILESYSTEM_DISK=local
QUEUE_CONNECTION=sync
SESSION_DRIVER=cookie
SESSION_LIFETIME=120
SESSION_ENCRYPT=true
SESSION_PATH=/
SESSION_DOMAIN=null

# Email
MAIL_MAILER=smtp
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=tu-email@gmail.com
MAIL_PASSWORD=tu-app-password
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS="tu-email@gmail.com"
MAIL_FROM_NAME="${APP_NAME}"

# Logging (optimizado para Vercel)
LOG_CHANNEL=stderr
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=error

# Configuraciones específicas para Vercel
VERCEL=1
VERCEL_ENV=production

# ===========================================
# INSTRUCCIONES PARA VERCEL:
# ===========================================
# 
# 1. PREPARAR PROYECTO:
#    - Asegúrate de tener vercel.json y api/index.php
#    - Instalar Vercel CLI: npm i -g vercel
# 
# 2. BASE DE DATOS EXTERNA:
#    - Crear cuenta en https://neon.tech (PostgreSQL gratuito)
#    - Crear nueva base de datos
#    - Copiar string de conexión
# 
# 3. DESPLEGAR:
#    - Ejecutar: vercel --prod
#    - Configurar variables en dashboard de Vercel
# 
# 4. CONFIGURAR VARIABLES:
#    - Ir a Settings > Environment Variables
#    - Agregar cada variable para Production
# 
# 5. LIMITACIONES:
#    - Funciones serverless (sin estado persistente)
#    - Tiempo límite de ejecución: 10 segundos
#    - Tamaño de respuesta: 4.5MB
#    - No incluye almacenamiento de archivos
# 
# ===========================================